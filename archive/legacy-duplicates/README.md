# Legacy Duplicates Archive Plan

This directory is reserved for duplicate/legacy roots that should not be used for deploy.

Current legacy duplicate identified:

- `f:/qt33.in/unified_msedcl_workspace/unified_msedcl_workspace/qt33`

Safety status:

- `qt33/.project-root.json` sets `deployAllowed=false`.
- `qt33/package.json` scripts now include guard checks that block build/deploy flow.

No files were deleted in this phase.
