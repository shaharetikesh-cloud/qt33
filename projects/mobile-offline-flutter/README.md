# Mobile Offline Flutter Project

Current root:

- `f:/qt33.in/unified_msedcl_workspace/unified_msedcl_workspace/qt33_flutter`

Includes:

- Flutter source (`lib`)
- Flutter Android project (`android`)
- Offline-first mobile runtime

Guard marker:

- `qt33_flutter/.project-root.json` with `projectId=mobile-offline-flutter`
