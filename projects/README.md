# Project Segregation Map

This folder defines the canonical three-project split without deleting any data.

- `web-connected` -> deployed web app + connected Capacitor APK (canonical)
- `mobile-offline-flutter` -> standalone Flutter offline APK
- `excel-vba-offline` -> standalone Excel/VBA offline system

Current source locations are documented in each project README.
